name: CI

on:
  pull_request:
    branches: [main]

jobs:
  typecheck:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Install Linux Rollup native binary
        # The lock file is generated on Windows and omits platform-specific
        # optional binaries. This step explicitly restores the Linux one.
        run: npm install --no-save @rollup/rollup-linux-x64-gnu

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run TypeScript check
        run: npm run lint

  test:
    name: Run Vitest Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Install Linux Rollup native binary
        # The lock file is generated on Windows and omits platform-specific
        # optional binaries. This step explicitly restores the Linux one.
        run: npm install --no-save @rollup/rollup-linux-x64-gnu

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run tests
        run: npm run test
